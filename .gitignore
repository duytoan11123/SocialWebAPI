# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs
.vscode/
.idea/
*.iml

# OS files
.DS_Store
Thumbs.db

# Logs
logs
*.log
npm-debug.log*

# Uploads (if stored locally and unnecessary to commit)
# uploads/
# But user might want to keep basic structure, so maybe ignore content but keep folder
# For now, standard nodejs ignore is sufficient.
